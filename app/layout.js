import "./globals.css";

export const metadata = {
  title: "Cursor Creature Adoption Agency",
  description: "Meet, test compatibility with, name, and adopt a tiny creature that lives near your cursor.",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
